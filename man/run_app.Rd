% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_app.R
\name{run_app}
\alias{run_app}
\title{Run the classroom brainwriting application}
\usage{
run_app(
  db_path = Sys.getenv("DB_PATH", "data/brainwriting.sqlite"),
  mod_pin = Sys.getenv("MOD_PIN", "635"),
  base_url = Sys.getenv("BASE_URL", "http://localhost:3838"),
  port = Sys.getenv("PORT", "3838"),
  host = "0.0.0.0",
  poll_ms = 2500
)
}
\arguments{
\item{db_path}{Main SQLite file path. Defaults to \code{DB_PATH}, then
\code{"data/brainwriting.sqlite"}.}

\item{mod_pin}{Moderator PIN. Defaults to \code{MOD_PIN}, then \code{"635"}.
Set a private PIN before deployment.}

\item{base_url}{Public participant URL used in the QR code. Defaults to
\code{BASE_URL}, then \code{"http://localhost:3838"}.}

\item{port}{Listening port. Defaults to \code{PORT}, then \code{3838}.}

\item{host}{Listening address, default \code{"0.0.0.0"}.}

\item{poll_ms}{Database polling interval in milliseconds, default \code{2500}.}
}
\value{
Invisibly, the value returned by \code{\link[shiny:runApp]{shiny::runApp()}} when the app stops.
}
\description{
Starts one multilingual Shiny application backed by SQLite. Open
\code{base_url} for participants and append \verb{/?mod=1} for the moderator.
Configuration is resolved at invocation, never at package load time.
}
\details{
The main database holds the standard session behind \code{base_url} and the
catalog of all further sessions. Each further session is a separate SQLite
file in a \code{sessions} folder next to the main database, with its own address
(\verb{?s=<code>}) and QR code. Moderators create, open, restart, archive and
delete sessions in the overview at \code{?mod=1&view=sessions}.

Use exactly one R process per data folder. No replicas or horizontal
scaling are supported. A round advances on the next connected client's poll;
with no clients connected it advances when someone reconnects. Moderator
logins are kept in server memory, so a restart asks for the PIN again.
}
\examples{
if (interactive()) {
  run_app()
}
}
